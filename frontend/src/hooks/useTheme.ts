/**
 * @deprecated Import { useTheme } from "@/providers/ThemeProvider" instead.
 * This re-export remains for backward compatibility during migration.
 */
export { useTheme } from "@/providers/ThemeProvider";
