// ---------------------------------------------------------------------------
// Convenience re-export — the authoritative hook lives in DocumentProvider.
// This file exists so consumers can import from hooks/ without coupling to
// the provider implementation path.
// ---------------------------------------------------------------------------

export { useDocuments } from "@/providers/DocumentProvider";